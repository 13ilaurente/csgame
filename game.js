import menuScene from './scenes/menuScene';

const config = {
  type: Phaser.AUTO,
  width: 272, /* 816 */
  height: 160, /* 480 */
  scene: [ menuScene ]
};

let game = new Phaser.Game(config);



